# Hashing

## Prerequisites

- Finish `05_Arrays/` basics first.
- You should already understand arrays, frequency counting, and simple lookup problems.

- `hashing.cpp`: frequency counting for numbers and characters using arrays and maps.

Study this after arrays because the same frequency ideas show up in many array problems.
